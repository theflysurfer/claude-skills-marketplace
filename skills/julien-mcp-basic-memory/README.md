# MCP Basic Memory

Persistent memory storage between Claude sessions.

## Installation
```
/plugin install path/to/julien-mcp-basic-memory
```

## Tools
- `memory_store` - Store information
- `memory_retrieve` - Retrieve information
- `memory_search` - Search in memory
- `memory_delete` - Delete an entry

## Requirements
- `uvx` installed
